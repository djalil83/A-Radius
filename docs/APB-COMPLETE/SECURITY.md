# APB Security
Credentials for MikroTik API, VPN, RADIUS, SNMP, OLT and Payment Gateways
must be encrypted at rest and never returned as plaintext to the frontend.
All sensitive operations require server-side authorization and Audit Trail.
